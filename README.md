# AIRS Website Redesign

## Run the project

```bash
npm install
npm run dev
```

Open the URL Vite shows.

If localhost causes issues:

```bash
npm run dev -- --host 127.0.0.1 --port 3000
```

Then open:

```text
http://127.0.0.1:3000/
```
